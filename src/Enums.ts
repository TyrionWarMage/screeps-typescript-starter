export enum Worktype {
    HARVEST,
    UPGRADE,
    BUILD,
    REPAIR,
    PAUSE,
    CARRY,
    MINER,
    SCOUT,
    COMBAT,
    CLAIMER,
    DEFENDER,
}

export enum Finalize {
    BUILD_TARGETED_ROAD,
    BUILD_TARGETED_CONTAINER,
    NO_FINALIZE
}
